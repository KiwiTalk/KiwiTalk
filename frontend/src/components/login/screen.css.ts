import { style } from '@vanilla-extract/css';

export const contentContainer = style({
  left: 0,
  top: 0,
  margin: '3rem 3rem',

  width: 281,
});

export const textLogo = style({
  marginBottom: 54,
  color: 'black',
});

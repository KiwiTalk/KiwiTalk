import { style } from '@vanilla-extract/css';

export const errorMessage = style({
  color: 'red',
});

export const resetText = style({
  color: '#4D5061',
  textAlign: 'center',
  userSelect: 'none',
});
